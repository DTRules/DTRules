# Go binaries
/dtrules
/api
/excel2dt
/excel2edd
/build/
*.exe
*.test
*.out
vendor/

# General
~*
*~
.~*
._*
*.log
*.o
*.so
*.dll
.DS_Store
.DS_Store?
ehthumbs.db
Icon?
Thumbs.db

# IDE
.idea/
.vscode/
*.settings
*.project

# Legacy Java
*.class
*.jar
target/
.metadata
pom.xml.releaseBackup

# DTRules specific
testfiles/output/*.*
output/
temp/
bin/
Uncompiled_DecisionTables.xml
Uncompiled_Edd.xml
*_dt_strip.xml
*_edd_strip.xml
*.fixedCounts

# Legacy directories
web-app/
apidocs
classes
generated-sources
surefire-reports
test-classes
KidAid/testfiles/output/*.*

# Generated run artifacts under a project's testfiles/, but NOT the scenarios
# themselves. This was a blanket `testfiles/` and it silently kept five sample
# scenarios out of the repo — the tests that consume them passed locally and
# could never have passed anywhere else (#865).
testfiles/output/
testfiles/**/*.trace.xml

# Sync manifest (local state)
.dtrules-manifest.json

# dtrules review cache (last-review.json) — local, regenerated by `dtrules review`
.dtrules/

# Python
.venv/
__pycache__/
*.pyc

# DTRules sync manifest (local state)
.sync-manifest.json

# Working docs (plans, dev docs, benchmarks)
plans/
docs-dev/
benchmark-results/
scripts/
sampleprojects/TaxReturn/.dtrules/

# release build output
dist/
