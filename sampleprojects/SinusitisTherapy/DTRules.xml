<DTRules>
  <!--
    Project marker. Its presence is what makes this directory a project to
    `dtrules edit` and to project discovery (#941/#942); <entry> names the
    table `dtrules run`, `dtrules debug` and the editor start from.

    Determine_Therapy orchestrates the three services: medication and dosing,
    creatinine clearance, and the interaction check.

    The patient fields carry `collect` metadata, so this is also the project
    behind the interactive and web interview modes of `dtrules run`.
  -->
  <entry>Determine_Therapy</entry>
</DTRules>
